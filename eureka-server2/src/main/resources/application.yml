server:
  port: 7002
eureka:
  instance:
    hostname: eureka2.com
  client:
    register-with-eureka: false #false 表示不向注册中心注册自己
    fetch-registry: false #false 表示自己端就是注册中心 并不去维护实例 并不需要去检索
    service-url:
     defaultZone: http://eureka.com:7000,http://eureka1.com:7001  #eureka 交互地址


